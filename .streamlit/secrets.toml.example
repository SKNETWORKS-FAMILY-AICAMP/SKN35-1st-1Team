# 이 파일을 secrets.toml 로 복사한 뒤 실제 값을 채우세요. (secrets.toml은 커밋 금지)
# MySQL 연결 정보가 있으면 앱이 자동으로 DB 모드로 전환됩니다.
# 없으면 lib/sample_data.py 의 샘플 데이터로 동작합니다.

[mysql]
host = "localhost"
port = 3306
user = "root"
password = "여기에_비밀번호"
database = "vehicle_support"
