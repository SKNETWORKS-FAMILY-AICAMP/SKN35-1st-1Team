# MySQL 접속 정보 — 이 파일을 .env 로 복사한 뒤 값을 채운다.
# .env 는 절대 git에 커밋하지 않는다 (.gitignore 등록됨).
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=seoul_calltaxi
MYSQL_CHARSET=utf8mb4
